#include <iostream>
using namespace std;
int main(){
    int size;
    cin>>size;
    int arr[size];
    for(int i=0; i<size; i++){
        cin>>arr[i];
    }
    sort(arr,arr+size);
   for(int k=0; k<size; k++){
    for(int i=k+1; i<size; i++){
        if(arr[k]==arr[i]){
            cout<<arr[k];
        }
    }
   } 
}
